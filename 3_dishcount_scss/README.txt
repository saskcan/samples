File:
'bootstrap_overrides.css.scss'

Overrides some default styles from Twitter Bootstrap ("http://getbootstrap.com/"). Written in SCSS and uses Rails helpers for sourcing fonts. Includes SCSS mixins and custom variables as well as CSS media queries.