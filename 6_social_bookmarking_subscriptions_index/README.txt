File:
'application_helper.rb'

The sortable method returns a link used for clickable column headers.

File:
'tables.js.coffee'

Based on css class, up or down arrows are added to the sorted column heading.

File:
'index.html.haml'

Index view for an authenticated user's task subscriptions. Uses Zurb Foundation front-end framework.